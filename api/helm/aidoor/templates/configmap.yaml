apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aidoor.fullname" . }}-config
  labels:
    {{- include "aidoor.labels" . | nindent 4 }}
data:
  appsettings.json: |-
    {
      "Logging": {
        "LogLevel": {
          "Default": "{{ .Values.config.logging.level }}",
          "Microsoft.AspNetCore": "Warning"
        }
      },
      "ConnectionStrings": {
        "Redis": "{{ include "aidoor.redisHost" . }}",
        "DefaultConnection": "Server=prod-db;Port=3306;Database=AIDoorDb;User=ss;Password=qweasd123!;"
      },
      "AllowedHosts": "*"
    } 